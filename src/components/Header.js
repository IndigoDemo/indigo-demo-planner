import React from 'react';

const Header = () => (
    <header className="navbar navbar-expand-lg navbar-light" style={styles}>
        <div className="navbar-brand text-white">
            Indigo Demo Planner
        </div>
    </header>
);

const styles = {
    backgroundColor: 'indigo',
};

export default Header;
